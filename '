#include <native/common.h>
#include <native/native.h>
struct nat_Syscall_Request {
    enum {
        fprintf,
    }func_type;
    void* arg;
    union {
        typeof(fprintf) nat_fprintf;
    };
}

struct nat_SyscallQueue {
    u64 len;
    u64 cap;
};
